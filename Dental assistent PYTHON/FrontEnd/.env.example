# Backend API URL
# Default: http://127.0.0.1:9000
# VITE_API_URL=http://127.0.0.1:9000

# Development API Key (for browser development only)
# This is only used when running in browser (not in Tauri app)
# In production Tauri app, the API key comes from the backend
# IMPORTANT: Never commit the actual .env file with real keys!
# VITE_DEV_API_KEY=your-dev-api-key-here
